catalog_version = 1
namespace = "gen3"
resource_api = "1.0.0"

# Shared entries are added only when Emerald and a future Gen3 game adapter
# agree on one semantic role, type, and schema. M0 intentionally leaves this
# catalog empty rather than guessing at cross-game equivalence.
