# R13-H7 — Battle Family Physical Isolation Report (compiled-H removal + ROM_BASE_ONLY ownership flip)

Stage: **COMPLETE** (2026-08-24) · STOP after this stage: no commit, no R13-I.

## 1. Result

The compiled H battle-family payloads are physically removed from the
linux64 native link. All five families (battle, battle-anim, field-effect,
battle-ai, contest-ai) now run exclusively from the live H arenas, with
ownership flipped 2,089/2,089 to `ROM_BASE_ONLY` (0
`COMPILED_PENDING_MIGRATION`). Isolation sweeps report 0 unexplained
bytes/symbols/relocs; the live range invariant (6,382/8,192) is unchanged;
the pack is byte-identical to the H2 pin (23,069 entries / 15,278,272 B /
`b711d358…`); State-v5 is untouched; the full battery is green under
ASan/UBSan; release size shrinks **−267,768 B** (23,838,616 B) and
DINFO **−317,152 B** (36,520,488 B) vs the H6 baselines.

## 2. Files changed

| File | Change |
|---|---|
| `Makefile_pc` | H7 carve: `DATA_ASM_OBJS` filter-out of the 6 H script objects + `.PHONY sweep-stale-h-script-objects` (rm -f) wired into `rom:` deps, both inside the `ifeq ($(NATIVE_LINUX)-$(LINUX64),1-1)` gate (lines 311–362) |
| `resources/extraction/emerald/bpee01/battle/modules/ownership.generated.toml` | 2,089 records flipped to `native = "ROM_BASE_ONLY"`, 0 COMPILED_PENDING_MIGRATION, `gba = "COMPILED"` preserved (regenerated by `gen_gameplay_family.py`) |
| `tests/run_emerald_native_asset_isolation.sh` | battle family wired into the isolation runner (ownership argv, scans, PASS line) + `battle_byte_exemptions` (4 sha-keyed records) + zero-width-alias handling + 4-byte scan floor |
| `tests/emerald_battle_live_test.c` | H7 fault matrix (22 cases) incl. the loader-rollback staging sequence (TryInitialize → RegisterRanges → Publish after `ClearMigratedEntries`) |
| `tests/run_emerald_battle_live.sh` | H7-FAULTS stage pinned at `passed=22` |
| `tests/run_emerald_battle_module_loader.sh` | ownership-state pin flipped to the H7 truth: 2,089 × ROM_BASE_ONLY (was the H6-era COMPILED_PENDING_MIGRATION assertion) |
| `docs/R13H7_BATTLE_SCRIPT_ISOLATION_REPORT.md` | this report |

## 3. Freeze (brief §1)

H6 baseline frozen before any removal: H6 oracle pins (2,351 AI
instructions / 1,738 battle-ai + 613 contest-ai / 1,586 AI relocs / 38
data-targets / 64 AI routing rows, both layouts), live ranges exactly
6,382/8,192 with the 5 H ranges, pack SHA-256 `b711d358…` (23,069),
release 24,106,384 B / DINFO 36,837,640 B, `--verify-game-data`
`f3ae088181bf583e55daf962a92bb46f4f1d07b7` exit 0. The carve was the only
change after the freeze; the pack and State-v5 format were never touched.

## 4. Remove/retain census (brief §2)

Compiled-H payload census against the frozen ownership:

| Quantity | Value |
|---|---:|
| Battle modules (semantic) | 2,089 |
| — battle-script | 645 (640 payload + 5 aliases) |
| — battle-anim-script | 658 (655 + 3) |
| — field-effect-script | 68 |
| — battle-ai | 553 |
| — contest-ai | 165 |
| Payload modules (bytes exist) | 2,081 |
| Zero-width aliases (semantic only) | 8 |
| Canonical payload bytes | 90,047 B |
| Relocations | 7,549 |
| Unexplained ownership | 0 |

## 5. Physical removal (brief §3)

The G6 carve template, applied to the H script objects under the linux64
gate (`ifeq ($(NATIVE_LINUX)-$(LINUX64),1-1)` in Makefile_pc):

- `DATA_ASM_OBJS` filter-out: `battle_scripts_1.o`, `battle_scripts_2.o`,
  `battle_ai_scripts.o`, `contest_ai_scripts.o`,
  `battle_anim_scripts.o`, `field_effect_scripts.o` (lines 311–329).
- `.PHONY sweep-stale-h-script-objects`: `rm -f` of the 6 objects,
  wired into the `rom:` prerequisites (lines 349–361), so a stale object
  cannot survive into the link.
- Windows/linux GBA toolchains untouched: the gate is linux64-only.

Post-carve `build/linux64/data/` contains 0 of the 6 objects.

## 6. Ownership flip (brief §4)

- `native = "ROM_BASE_ONLY"`: **2,089 / 2,089**
- `COMPILED_PENDING_MIGRATION`: **0**
- `gba = "COMPILED"`: **2,089 / 2,089** (GBA build keeps the compiled
  payloads; source-level intact per the R7B/R8 GBA-branch policy).

## 7. Zero-width aliases (brief §5)

The 8 zero-width alias records (encoded_length 0, `source_encoded_sha256`
= sha256 of empty) are semantic-only: they name canonical words the
compiled ROM never materialized (e.g. `effect-protect`, `move-none`
slots). They carry no payload, no artifact file, and no pack record
(pack 23,069 = 20,988 + 2,081). The isolation runner skips artifact
reads/byte scans for them and keeps the symbol-absence check; all 8 pass.

## 8. Per-family removal (brief §6–§10)

Each family's compiled objects are gone from the link; each family's
payload/bytecode remains live in its H arena (families 1–5), proven by
the post-carve battery (sec 21):

- battle (645): compiled `battle_scripts_1.o` + `battle_scripts_2.o`
  removed; 199 label words resolve via `ResolveCompiledLabel`; routing
  rows republicate from arena bytes (H5 oracle 238 rows).
- battle-anim (658): `battle_anim_scripts.o` removed; H4 oracle 2,081
  byte-exact modules, 7,549 relocs.
- field-effect (68): `field_effect_scripts.o` removed.
- battle-ai (553): `battle_ai_scripts.o` removed; H6 oracle 1,738
  instructions.
- contest-ai (165): `contest_ai_scripts.o` removed; 613 instructions.

## 9. Routing tables (brief §11)

The 13 routing-kind modules in `boundary_maps.generated.toml` and the
12-member `ROUTING_TABLES` enum (7 battle/AI + 5 anim/FE) are untouched.
Routing republication is arena-sourced (H5 oracle swept-words 2,587;
H6 2,051 non-reloc words verified non-target). The quiet-BGM u16
(13th, `EMERALD_BATTLE_QUIET_BGM_WORD`) is a data word, not an enum
member; it resolves via `ResolveModuleData` — unchanged by the removal.
No broad routing exemption was introduced.

## 10. Engine semantic bindings preserved (brief §12)

The seam's live machinery is retained in the binary (verified by nm):

```
kEmeraldBattleLiveTable                0xc5fc40 R
EmeraldBattleLive_ResolveCompiledLabel 0x7840b0 T
EmeraldBattleLive_ResolveRoutingTarget 0x783eb0 T
EmeraldScriptCompat_ResolveRoutingTable 0x7c8380 T
```

EWRAM bases, callbacks, sprite templates, gfx, and engine tables were
never candidates for removal. The only bytes removed are the compiled
script payloads and their tables.

## 11. Binary isolation sweep (brief §13/§14)

Full re-run of `tests/run_emerald_native_asset_isolation.sh --binary
pokeemerald-linux64` post-carve, after artifact restoration (1,804
trainer/pokemon graphics artifacts sha-pinned back from the qualified
reference tree) and runner fixes:

```
native asset-isolation: 33521 ok, 0 failed
PASS: native target matches ownership (7908/7908 ROM_BASE_ONLY isolated
- 196 trainer + 1608 pokemon battle + 288 object-event + 1544 tileset +
882 layout + 1301 audio + 2089 battle modules; 1057/1057
COMPILED_PENDING_MIGRATION present - 1055 movement + 2 multiboot)
```

Sweep evidence per record: artifact sha verification (encoded/decoded),
symbol absence via nm (all 2,088 battle symbols absent), encoded and
decoded byte scans, object scan for the checked binary's obj dir. The
battle family contributes **0 unexplained** findings; the 4 sha-keyed
exemptions are classified in sec 24. Short (1–3-byte) payloads use the
G6 scan floor (symbol sweep is the hard proof); zero-width aliases are
skipped by construction.

## 12. No-fallback proof (brief §15)

No compiled fallback exists: the 6 compiled objects are not in the
link; the 8 compiled family tables are absent from the binary (nm);
all 2,088 legacy symbols absent; the H7 fault matrix (sec 20) proves
the live seam fail-closes (NOT_PUBLISHED / TARGET_UNRESOLVED /
BOUNDARY_INVALID) when a compiled symbol is demanded — there is nothing
left to fall back to.

## 13. Live range invariant (brief §16)

6,382 / 8,192 ranges, unchanged; the 5 H ranges (families 1–5)
unchanged; H4 generation replacement 6,382 → 6,377 on B. The removal
does not touch the range model.

## 14. State-v5 invariants (brief §17)

State-v5 format untouched (no resource pointer enters any serialized
slice; the live tables sit in host .data outside every slice). The H3/H5
nested blocking fresh-process proofs re-ran green post-carve (sec 21).

## 15. Pack invariant (brief §18)

| Quantity | Value |
|---|---:|
| Pack entries (header offset 28) | **23,069** |
| Pack size | 15,278,272 B |
| Pack SHA-256 | `b711d35877332c43ce671caacda7640905205579a367e8d60bc683c5d0aac5bb` |
| Canonical battle payload | 90,047 B (sum of `encoded_length` over 2,089 records) |
| Determinism | byte-identical to the H2 pin; pack not regenerated by H7 |

## 16. Binary-size proof (brief §19)

| Build | H6 baseline | H7 | Delta |
|---|---:|---:|---:|
| Release (`-B DINFO=0 rom`) | 24,106,384 B | **23,838,616 B** | **−267,768 B** (material shrink) |
| DINFO (`-B DINFO=1 rom`) | 36,837,640 B | **36,520,488 B** | **−317,152 B** (material shrink) |

## 17. Forced builds (brief §20/§21)

The mandated form only: `make -f Makefile_pc NATIVE_LINUX=1 LINUX64=1 -B rom`
(and the `DINFO=1` variant). Never `make -B linux64` (no such goal).

Both builds: `--verify-game-data` → `f3ae088181bf583e55daf962a92bb46f4f1d07b7`,
exit 0. Release binary left on disk.

## 18. Differential VM regressions (brief §22)

The full live suite re-ran post-carve with all H4/H5/H6 pins held:
H4 oracle both layouts (2,043 entry words / 2,081 byte-exact / 7,549
relocs / 3,798 script-targets / 20 refuse-only / 8 gaps), H5 oracle
both layouts (238 routing rows / 199 labels / 50 EWRAM / 2,587 swept
words), H5-249 (3,390 decoded / 238 + 11 slots / 2 executed), H6 AI
oracle both layouts (2,351 instructions / 1,738 + 613 / 1,586 relocs /
38 data-targets / 64 routing rows / 2,051 swept) — all green, exit 0.

## 19. Real integration (brief §23)

Headless integration on the post-carve release build: the game boots
and `--verify-game-data` exits 0 (pack open + catalog reconstruction +
live registration all succeed on the carved binary), and the H7 fault
matrix exercises the full live integration surface — registration,
routing republication, label resolution, module execution — for all
five families. The H7 removal is byte-payload-only (execution-dead
since H5/H6); interactive play remains the §25 manual checklist.

## 20. Fault matrix (brief §24)

H2–H6 matrices held (staging/four-way, boundaries, H4 11/11, H5 8/8,
H6 AI 10/10) plus the new H7 matrix — **22/22**:

1. Pre-publish access → NOT_PUBLISHED (2 cases)
2. SetupScriptCompatSession → 6,382 ranges, then ClearMigratedEntries
   rolls the loader publication back; explicit seam staging
   (TryInitialize → RegisterRanges → Publish) re-publishes
3. Host-pointer injection (module-0 word) → refused
4. Bytecode-root-as-routing → BOUNDARY_INVALID
5. ResolveModuleData bytecode refusal
6. quiet-bgm word +4 interior → refusal
7. 0x08000000 → TARGET_UNRESOLVED (2 cases)
8. 12 routing rows: H4-layout-span containment + row bounds
   (anim-moves rowCount == 356)
9. quiet-BGM: 8-byte span + 0xFFFF sentinel
10. 199 label words: LabelAddress == word and resolve
11. Stale generation: TryInitialize with an old generation survives
    only until publication

Post-carve run: `H7-FAULTS passed=22` (the loader-rollback staging
block was required: `ClearMigratedEntries` rolls back the loader's
publication, leaving the seam unpublished until the seam drives its own
transactional sequence).

## 21. Regression battery (brief §25)

Full battery green post-carve (2026-08-24):

- **H4/H5/H6 live suite (sanitize, ASan/UBSan)**: oracles both layouts,
  faults 11/8/10/22, H4 replace 6,382→6,377 (identity unregister/
  restore), H4 anim fresh-process (module 553 ip 4 ret 19), H5 nested
  blocking fresh-process (module 1208 ip 3 rets 1253/5 + 1255/5,
  opcode 0x12), H5-249 (3,390 / 238 + 11 / 2), H6 state both families —
  all clean, exit 0.
- **Isolation sweep**: 33,521 ok, 0 failed (sec 11).
- **Battle-family general suites** (re-run post-carve, all exit 0):
  module-loader (incl. the H7 ownership-state pin: 2,089 ×
  ROM_BASE_ONLY, family counts 645/658/553/165/68, schemas 47-51,
  zero-width aliases 8, tamper refusals), battle-state,
  battle-state-faults (16/16), battle-state-cross-restart.

## 22. Ownership census (brief §26)

645 / 658 / 68 / 553 / 165 = **2,089 ROM_BASE_ONLY**, 0
COMPILED_PENDING_MIGRATION, 2,088 unique legacy symbols
(`AI_CheckBadMove` legitimately duplicated across battle-ai/contest-ai),
8 zero-width aliases. Verified by independent census of the generated
TOML (not just the runner's parse).

## 23. Alias/payload reconciliation (brief §27 — HARD GATE)

2,089 semantic = 2,081 payload + 8 zero-width aliases; pack 23,069 =
20,988 + 2,081 (aliases cannot be pack records — no payload); canonical
90,047 B; 7,549 relocs; H2 four-way oracle holds. **Gate passed.**

## 24. Named exclusions with owners (brief §28)

Four sha-keyed byte-scan exemptions for the battle family, all
classified at scan time by locating the exact bytes in the binary
(readelf VA map + nm bracketing):

| Record | Payload | Location | Owner / classification |
|---|---|---|---|
| `g-battlescripts-for-safari-actions` | 16 B (4 GBA words) | VA 0xc63540, inside `sScriptTargetWords` (battle_live_table.generated.c rodata, 0xc62300..0xc64060) | Seam §12 retention: the live target index retains canonical GBA words by design; compiled TU absent (nm + carve) |
| `g-battlescripts-for-running-by-item` | 4 B (0x082dbeb3) | VA 0xc60ac0, seam rodata (sLabels..sScriptTargetWords span) | Seam §12 retention: routing target word in the live bindings region |
| `hit-from-crit-calc` | 4 B (0x07060504) | VA 0x409fdf, .text near `ShuffleApprenticeSpecies` | R7B coincidence precedent: degenerate word collides with an x86 instruction run; not a GBA address |
| `selecting-imprisoned-move` | 4 B (0x4400ba11) | VA 0x4e4ec9, .text near `Cmd_typecalc` | R7B coincidence precedent (same class) |

The hard isolation proof for every battle record remains the 2,088-symbol
nm sweep + 6-object carve + size delta — the exemptions only classify
observed byte coincidence.

## 25. Manual DINFO checklist (brief §29 — 17 items)

Interactive on the DINFO build (release first, then DINFO):

1. wild battle enter/flee/win/lose — scripts execute from arena
2. trainer battle intro, dialogue, victory, return to field
3. double battle multi-target flow
4. AI move selection + switch logic (battle-ai + contest-ai)
5. damage/status/stat/faint/switch/victory script flows
6. ball throw / catch / safari actions
7. anims: every move anim + field effects (dig/fly/surf/rock-smash)
8. save/load mid-battle (AI quiescent at capture; stale IP never resumed)
9. item usage in battle (running-by-item routing rows)
10. imprison/disabled-move edge scripts
11. trainer-rematch / frontier flows
12. **resource missing after removal**: delete/rename a pack module → the
    seam fail-closes (NOT_PUBLISHED / TARGET_UNRESOLVED), no crash
13. **routing publication absent**: withhold a routing row → BOUNDARY_INVALID
14. **semantic binding missing**: query a label with the table withheld →
    TARGET_UNRESOLVED, no fallback
15. **compiled-symbol injection detector**: `nm` sweep on the DINFO
    binary — 0 compiled H symbols
16. **stale generation**: hold an old-generation snapshot across a
    reload → refused at publication
17. **partial ownership/publication mismatch**: catalog entry without a
    pack record → fail-closed at registration

## 26. Hard completion gates (brief §31 — 19 gates)

- ✓ freeze (sec 3) · ✓ census, unexplained 0 (sec 4) · ✓ carve (sec 5)
- ✓ ownership 2,089/0 ROM_BASE_ONLY, GBA COMPILED (sec 6)
- ✓ zero-width aliases semantic only (sec 7)
- ✓ per-family removal (sec 8) · ✓ routing census, no broad exemption (sec 9)
- ✓ engine bindings preserved (sec 10) · ✓ isolation sweep 0 unexplained (sec 11)
- ✓ false positives avoided (sec 11/24) · ✓ no fallback (sec 12)
- ✓ ranges 6,382/5 unchanged (sec 13) · ✓ State-v5 (sec 14)
- ✓ pack 23,069 / 90,047 B / deterministic (sec 15)
- ✓ size proof, material shrink (sec 16) · ✓ forced-build form (sec 17)
- ✓ release/DINFO verify f3ae0881… exit 0, release left on disk (sec 17)
- ✓ differential VM (sec 18) · ✓ real integration (sec 19)
- ✓ fault matrix 22/22 (sec 20) · ✓ full battery (sec 21)
- ✓ ownership census 645/658/68/553/165 (sec 22)
- ✓ alias/payload reconciliation (sec 23) · ✓ named exclusions (sec 24)

## 27. STOP conditions (brief §32)

None triggered: no size growth (material shrink, sec 16); no range
drift (6,382 constant); no ownership residue (0 pending); no pack
mutation (`b711d358…` held); no State-v5 change; no fallback required;
no unexplained isolation findings; no fault-matrix regression.

## 28. R13-I prerequisites

All five battle families are physically gone from the linux64 link,
fully arena-served. The next stages can proceed without touching the
battle payloads.

## Final pins

- verify-game-data: `f3ae088181bf583e55daf962a92bb46f4f1d07b7` exit 0
- pack: 23,069 entries · 15,278,272 B · `b711d35877332c43ce671caacda7640905205579a367e8d60bc683c5d0aac5bb`
- live ranges: 6,382/8,192 (5 H)
- canonical payload: 90,047 B · 2,081 payload + 8 aliases · 7,549 relocs
- isolation: 33,521 ok / 0 failed · 7,908/7,908 + 1,057/1,057
- fault matrices: 11/11 · 8/8 · 10/10 · **22/22**
- release: 23,838,616 B (H6 24,106,384 → −267,768) · DINFO: 36,520,488 B (H6 36,837,640 → −317,152), 7 debug sections
